import { LucideIcon } from "lucide-react";
import { IRHFProps } from "./types"

export interface IIPasswordInputProps extends IRHFProps {
  nameInput: string
  nameLabelInput: string
  namePlaceholderInput: string;
  Icon: LucideIcon;
}

